# LightLag
Solar system light-travel time visualizer for Earth-relative distances.

**Run**
1. Create a virtual environment and install dependencies.
2. Run the app.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 lightlag/app.py
```
